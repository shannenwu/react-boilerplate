const express = require("express");
const router = express.Router();

const Users = require("../models/user");

const v = require("./validators");

// get, create, signin, signout methods here.

module.exports = router;
